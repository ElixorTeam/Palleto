<AuthorizeView Policy="@PolicyEnum.Admin">
  <SectionInputWrapper
    Label="@Label"
    HtmlFor="@HtmlId()"
    ValueToCopy="@Value.ToString()"
  >
    <InputTextField
      id="@HtmlId()"
      Value="@Value.ToString().ToUpper()"
      Disabled
    />
  </SectionInputWrapper>
</AuthorizeView>

@code {
  [Parameter] public Guid Value { get; set; }
  [Parameter] public string Label { get; set; } = "UID";

  private string HtmlId() => $"uid-{Value}";
}