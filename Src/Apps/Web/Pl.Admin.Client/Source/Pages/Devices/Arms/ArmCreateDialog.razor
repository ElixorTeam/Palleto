@implements IDialogContentComponent

<SectionDialog>
  <Tab Id="@typeof(ArmCreateForm).ToString()" Title="@Localizer["SectionArm"]">
    <ArmCreateForm FormModel="@new()" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;
}
