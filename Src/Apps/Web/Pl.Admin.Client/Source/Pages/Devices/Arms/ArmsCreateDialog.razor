@implements IDialogContentComponent

<SectionDialog>
  <Tab Id="@typeof(ArmsCreateForm).ToString()" Title="@Localizer["SectionArm"]">
    <ArmsCreateForm FormModel="@new()" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;
}
