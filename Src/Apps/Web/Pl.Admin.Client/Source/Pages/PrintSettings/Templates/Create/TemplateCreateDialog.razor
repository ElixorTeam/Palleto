@implements IDialogContentComponent

<SectionDialog>
  <Tab Id="@typeof(TemplateCreateForm).ToString()" Title="@Localizer["SectionTemplates"]">
    <TemplateCreateForm FormModel="@new()" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;
}
