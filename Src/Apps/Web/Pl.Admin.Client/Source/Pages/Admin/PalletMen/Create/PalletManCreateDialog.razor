@implements IDialogContentComponent

<SectionDialog>
  <Tab Id="@typeof(PalletManCreateForm).ToString()" Title="@Localizer["SectionPalletMen"]">
    <PalletManCreateForm FormModel="@new()" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;
}
