@using Pl.Admin.Client.Source.Shared.Api.Web.Models
@using Pl.Admin.Client.Source.Shared.Services.Stores
@using Pl.Admin.Models.Features.Admins.Users.Commands

@implements IDialogContentComponent<UserModel>

<SectionDialog>
  <Tab Id="@typeof(UsersUpdateForm).ToString()" Title="@Localizer["SectionUsers"]">
    <UsersUpdateForm Model="Content" FormModel="@(new UserUpdateDto { ProductionSiteId = Content.ProductionSite.Id })" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;

  [Parameter] public UserModel Content { get; set; } = default!;
}
