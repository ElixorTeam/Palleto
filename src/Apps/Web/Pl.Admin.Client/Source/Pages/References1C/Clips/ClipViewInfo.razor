@inject IStringLocalizer<ApplicationResources> Localizer
@inject IStringLocalizer<WsDataResources> WsDataLocalizer

<ViewFieldGrid Label="@Localizer["FieldCommon"]">
  <ViewFieldGuid Value="@Model.Id" />

  <ViewField
    Label="@WsDataLocalizer["ColName"]"
    Value="@Model.Name"
  />

  <ViewField
    Label="@WsDataLocalizer["ColWeight"]"
    Value="@Model.Weight.ToString(CultureInfo.InvariantCulture)"
  />

</ViewFieldGrid>

@code {
  [Parameter, EditorRequired] public PackageDto Model { get; set; } = default!;
}