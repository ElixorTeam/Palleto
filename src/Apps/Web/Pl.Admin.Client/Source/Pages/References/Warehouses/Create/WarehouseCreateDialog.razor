@using Pl.Admin.Client.Source.Shared.Services.Stores
@implements IDialogContentComponent

@inject IStringLocalizer<ApplicationResources> Localizer
@inject IState<ProductionSiteState> ProductionSiteState

<SectionDialog>
  <Tab Id="@typeof(WarehouseCreateForm).ToString()" Label="@Localizer["SectionWarehouses"]">
    <WarehouseCreateForm FormModel="@(new() { ProductionSiteId = ProductionSiteState.Value.ProductionSite.Id })" />
  </Tab>
</SectionDialog>
