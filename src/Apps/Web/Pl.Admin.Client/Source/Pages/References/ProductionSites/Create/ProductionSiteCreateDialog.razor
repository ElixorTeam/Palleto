@implements IDialogContentComponent

@inject IStringLocalizer<ApplicationResources> Localizer

<SectionDialog>
  <SectionDialogTab Id="@typeof(ProductionSiteCreateForm).ToString()" Label="@Localizer["SectionProductionSites"]">
    <ProductionSiteCreateForm FormModel="@(new())" />
  </SectionDialogTab>
</SectionDialog>
